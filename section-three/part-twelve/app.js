// Local modules need to be required into the given files
// Globally don't get loaded in to source file. Instead if gives us access to a new command in the CLI
// To install globally: npm i nodemon -g
// Now it's installed globally and not affecting the package.json or package-lock.json
// CLI: nodemon app.js

console.log("WE'RE RUNNING")
console.log('AND WE MADE CHANGES!!!')
